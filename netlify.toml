[functions]
  directory = "netlify/functions"
  node_bundler = "esbuild"

# Admin paneli arama motorlarında görünmesin ve hiç önbelleğe alınmasın
[[headers]]
  for = "/admin/*"
  [headers.values]
    X-Robots-Tag = "noindex, nofollow"
    Cache-Control = "no-store"
